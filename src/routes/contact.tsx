import { createFileRoute } from '@tanstack/react-router';import { useServerFn } from '@tanstack/react-start';import { useState } from 'react';import { submitEnquiry } from '@/lib/contact.functions';import { CONTACT,whatsappLink,type Enquiry } from '@/lib/contact';import { SocialLinks } from '@/components/site/SocialLinks';export const Route=createFileRoute('/contact')({component:ContactPage});const empty:Enquiry={name:'',email:'',company:'',budget:'',message:''};function ContactPage(){const send=useServerFn(submitEnquiry);const [form,setForm]=useState(empty);const [status,setStatus]=useState('idle');const [error,setError]=useState('');const [sent,setSent]=useState(empty);const set=(k:keyof Enquiry)=>(e:React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement>)=>setForm(f=>({...f,[k]:e.target.value}));async function submit(e:React.FormEvent){e.preventDefault();if(status==='sending')return;const p={...form,name:form.name.trim(),email:form.email.trim(),company:form.company.trim(),budget:form.budget.trim(),message:form.message.trim()};if(!p.name||!p.email||p.message.length<5){setError('Please add your name, a valid email and a brief of at least 5 characters.');setStatus('failed');return}setStatus('sending');try{await send({data:p});setSent(p);setForm(empty);setStatus('sent')}catch(err){setError(err instanceof Error?err.message:'Unable to send');setStatus('failed')}}return <main className="mx-auto max-w-6xl px-5 py-16"><p className="num text-xs uppercase tracking-[0.2em] text-accent">Contact</p><h1 className="mt-3 text-3xl font-semibold md:text-5xl">Discuss a project</h1><p className="mt-4 max-w-2xl text-sm text-muted-foreground">Send a short brief — scope, timeline and what's currently going wrong.</p><div className="mt-12 grid gap-12 md:grid-cols-[1.2fr_1fr]"><form onSubmit={submit} className="space-y-4" noValidate><div className="grid gap-4 sm:grid-cols-2">{(['name','email','company','budget'] as const).map(k=><label key={k}><span className="num text-xs uppercase text-muted-foreground">{k}</span><input required={k==='name'||k==='email'} type={k==='email'?'email':'text'} value={form[k]} onChange={set(k)} className="hairline mt-2 w-full bg-surface px-3 py-2 text-sm"/></label>)}</div><label><span className="num text-xs uppercase text-muted-foreground">Project brief</span><textarea required rows={6} value={form.message} onChange={set('message')} className="hairline mt-2 w-full bg-surface px-3 py-2 text-sm"/></label><button disabled={status==='sending'} className="bg-primary px-6 py-3 text-sm font-semibold text-primary-foreground">{status==='sending'?'Sending…':'Send enquiry'}</button>{status==='failed'&&<div className="hairline bg-surface p-4 text-sm text-destructive">{error}</div>}{status==='sent'&&<div className="hairline bg-surface p-4 text-sm">Enquiry received. <a href={whatsappLink(sent)} target="_blank" rel="noreferrer" className="text-accent">Open WhatsApp manually</a></div>}</form><aside><div className="hairline bg-surface p-5"><SocialLinks className="flex-col"/></div><div className="hairline mt-6 bg-surface p-5"><p className="num text-xs uppercase text-muted-foreground">Phone</p><a href={`tel:+${CONTACT.phoneIntl}`} className="mt-3 block text-2xl text-primary">{CONTACT.phone}</a></div></aside></div></main>}
