import { createFileRoute } from "@tanstack/react-router";
import { CtaBand } from "../components/site/CtaBand";
export const Route=createFileRoute("/services")({component:ServicesPage});
const services=[['Project Management','Delivery leadership, planning, coordination, governance and decision support.'],['PMO Setup & Improvement','Practical PMO structures, standards, reporting packs, cadence and governance.'],['Project Controls','Schedule, cost, change, risk and performance controls designed around the project.'],['Reporting & Dashboards','Concise management information that turns project data into actions and decisions.']];
function ServicesPage(){return <div><main className="mx-auto max-w-6xl px-5 py-16 md:py-24"><p className="num text-xs uppercase tracking-[0.22em] text-accent">Services</p><h1 className="mt-5 max-w-3xl text-4xl font-semibold md:text-6xl">Practical controls for complex delivery.</h1><div className="mt-12 grid gap-px bg-border md:grid-cols-2">{services.map(([title,copy],i)=><article key={title} className="bg-background p-7"><div className="num text-xs text-muted-foreground">0{i+1}</div><h2 className="mt-5 text-xl font-semibold">{title}</h2><p className="mt-3 max-w-md text-sm leading-relaxed text-muted-foreground">{copy}</p></article>)}</div></main><CtaBand/></div>}
