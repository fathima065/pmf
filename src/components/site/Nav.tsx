import { Link } from '@tanstack/react-router';
import { useState } from 'react';
const links=[{to:'/',label:'Home'},{to:'/about',label:'About'},{to:'/services',label:'Services'},{to:'/dashboard',label:'Dashboard'},{to:'/contact',label:'Contact'}] as const;
export function Logo(){return <span className="font-display inline-flex items-center gap-2 text-lg font-bold"><span className="h-2 w-2 bg-primary"/>Fathima Np</span>}
export function Nav(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-border bg-background/85 backdrop-blur"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link to="/" onClick={()=>setOpen(false)}><Logo/></Link><nav className="hidden items-center gap-7 md:flex">{links.map(l=><Link key={l.to} to={l.to} activeOptions={{exact:l.to==='/'}} className="font-display text-sm text-muted-foreground hover:text-foreground">{l.label}</Link>)}<Link to="/contact" className="bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Discuss a project</Link></nav><button className="hairline h-9 w-9 md:hidden" aria-label="Toggle menu" onClick={()=>setOpen(v=>!v)}>☰</button></div>{open&&<nav className="border-t border-border bg-surface px-5 py-3 md:hidden">{links.map(l=><Link key={l.to} to={l.to} onClick={()=>setOpen(false)} className="block border-b border-border py-3 text-sm">{l.label}</Link>)}</nav>}</header>}
