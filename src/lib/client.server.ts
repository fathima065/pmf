import { createClient } from '@supabase/supabase-js';
export const supabaseAdmin=new Proxy({} as ReturnType<typeof createClient>,{get(_,prop,receiver){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY');return Reflect.get(createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}}),prop,receiver)}});
