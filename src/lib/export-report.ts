import type { ProjectData } from './dashboard-data';
function esc(v:string|number){const s=String(v);return /[",\n]/.test(s)?`"${s.replace(/"/g,'""')}"`:s}function row(c:(string|number)[]){return c.map(esc).join(',')}
export function buildProjectReportCsv(p:ProjectData,demo=true){const l=[row(['Project report',p.name]),...(demo?[row(['Data status','DEMO DATA — not real client information'])]:[]),row(['Client',p.client]),row(['Period',p.range]),row(['Schedule health',p.kpi.scheduleHealth]),row(['Budget utilisation (%)',p.kpi.budgetUsed]),row(['Budget approved',p.kpi.budgetTotal]),row(['Open risks',p.kpi.openRisks]),row(['Milestones',`${p.kpi.milestonesDone}/${p.kpi.milestonesTotal}`]),'',row(['Risks & issues']),row(['ID','Description','Owner','Severity','Status','Due'])];p.risks.forEach(r=>l.push(row([r.id,r.description,r.owner,r.severity,r.status,r.due])));return l.join('\n')}
export function downloadProjectReport(p:ProjectData,demo=true){const b=new Blob([`\uFEFF${buildProjectReportCsv(p,demo)}`],{type:'text/csv'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=`demo-${p.id}-report.csv`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(u)}
