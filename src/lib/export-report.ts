import type { ProjectData } from "./dashboard-data";
function esc(v:string|number){const s=String(v);return /[",\n]/.test(s)?`"${s.replace(/"/g,'""')}"`:s}
function row(c:(string|number)[]){return c.map(esc).join(',')}
export function buildProjectReportCsv(project:ProjectData,isDemo=true){const lines:string[]=[];lines.push(row(["Project report",project.name]));if(isDemo)lines.push(row(["Data status","DEMO DATA — not real client information"]));lines.push(row(["Client",project.client]),row(["Period",project.range]),row(["Generated",new Date().toISOString()]),'',row(["KPIs"]),row(["Schedule health",project.kpi.scheduleHealth]),row(["Schedule note",project.kpi.scheduleNote]),row(["Budget utilisation (%)",project.kpi.budgetUsed]),row(["Budget approved",project.kpi.budgetTotal]),row(["Open risks",project.kpi.openRisks]),row(["Milestones",`${project.kpi.milestonesDone}/${project.kpi.milestonesTotal}`]),'',row(["Workstreams"]),row(["Name","Start week","Span (weeks)","Actual %","Planned %"]));project.workstreams.forEach(w=>lines.push(row([w.name,w.start+1,w.span,w.progress,w.planned])));lines.push('',row(["Risks & issues"]),row(["ID","Description","Owner","Severity","Status","Due"]));project.risks.forEach(r=>lines.push(row([r.id,r.description,r.owner,r.severity,r.status,r.due])));return lines.join('\n')}
export function downloadProjectReport(project:ProjectData,isDemo=true){const blob=new Blob([`\uFEFF${buildProjectReportCsv(project,isDemo)}`],{type:'text/csv;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${isDemo?'demo-':''}${project.name.toLowerCase().replace(/[^a-z0-9]+/g,'-')}-report-${new Date().toISOString().slice(0,10)}.csv`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
